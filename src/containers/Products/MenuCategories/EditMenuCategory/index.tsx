import { memo } from 'react';

import InputsTable from '../components/InputsTable';

const EditMenuCategory = () => {
  return (
    <InputsTable title="Edit Category" />
  );
};

export default memo(EditMenuCategory);
